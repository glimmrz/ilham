/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "zeris.vercel.app",
      "algomart-five.vercel.app",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
